#include <stdio.h>
#include <math.h>

int main(){
    long long n;
    if(scanf("%lld",&n)!=1) return 0;
    if(n<0){ printf("Not perfect square\n"); return 0; }
    long long r = (long long)floor(sqrt((double)n)+0.5);
    if(r*r==n) printf("Perfect square\n"); else printf("Not perfect square\n");
    return 0;
}
